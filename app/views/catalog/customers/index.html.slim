= provide(:title, "Справочник: контрагенты")

= content_for :control_panel do
	== render "shared/control_panel", panel: {btn: {add: {title: "Добавить контрагента", action: new_catalog_customer_path}}}

ul.media-list id="list" 
	== render @items
	= will_paginate @items, class: "pagination pagination-centered"