<!DOCTYPE html>
<html>
<head>
	<title>Система учета талонов | <%= yield(:title) %></title>
	<%= stylesheet_link_tag    "application", :media => "all" %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
</head>
<body>
	<header class="navbar navbar-fixed-top custom-navigation">
		<nav class="navbar-inner custom-navigation">
			<div class="container">
				<%= render "layouts/navigation" %>
			</div>
		</nav>
	</header>
	<div class="row">
		<div class="span12" style="float: none; margin: 0 auto;">
			<%= render "layouts/messages" %>
			<%= yield %>
		</div>
	</div>
</body>
</html>